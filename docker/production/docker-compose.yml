version: '3'

services:
  with-docker-multi-env-production:
    build:
      context: ../../
      dockerfile: docker/production/Dockerfile
    image: sugar-dating-fe-production
    ports:
      - '3003:3000'
